add_swift_library(IndexStoreDBTests
  SHARED
  MODULE_NAME
  IndexStoreDBTests
  MODULE_LINK_NAME
  IndexStoreDBTests
  MODULE_PATH
  ${CMAKE_BINARY_DIR}/swift/IndexStoreDBTests.swiftmodule
  SOURCES
  IndexStoreDBTests.swift 
  LocationScannerTests.swift
  IndexTests.swift	
  XCTestManifests.swift
  TARGET
  ${CMAKE_C_COMPILER_TARGET}
  CFLAGS
  ${deployment_target}
  LINK_FLAGS
  ${libdispatch_ldflags}
  ${foundation_ldflags}
  ${xctest_ldflags}
  -L;${CMAKE_BINARY_DIR}/lib
  SWIFT_FLAGS
  -I;${CMAKE_SOURCE_DIR}/include/CIndexStoreDB
  ${libdispatch_cflags}
  ${foundation_cflags}
  ${xctest_cflags}
  -I;${CMAKE_BINARY_DIR}/swift
  DEPENDS
  CIndexStoreDB
  IndexStoreDB
  ISDBTestSupport
  ISDBTibs
  )
