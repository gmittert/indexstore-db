add_swift_library(ISDBTestSupport
  SHARED
  MODULE_NAME
  ISDBTestSupport
  MODULE_LINK_NAME
  ISDBTestSupport
  MODULE_PATH
  ${CMAKE_BINARY_DIR}/swift/ISDBTestSupport.swiftmodule
  SOURCES
  TestLocationScanner.swift
  XCTestAssertions.swift
  SourceFileCache.swift
  TestSources.swift
  TestLocation.swift
  TibsTestWorkspace.swift
  TARGET
  ${CMAKE_C_COMPILER_TARGET}
  CFLAGS
  ${deployment_target}
  LINK_FLAGS
  ${libdispatch_ldflags}
  ${foundation_ldflags}
  ${xctest_ldflags}
  -L;${CMAKE_BINARY_DIR}/lib
  SWIFT_FLAGS
  -I;${CMAKE_SOURCE_DIR}/include/CIndexStoreDB
  ${libdispatch_cflags}
  ${foundation_cflags}
  ${xctest_cflags}
  -I;${CMAKE_BINARY_DIR}/swift
  DEPENDS
  CIndexStoreDB
  IndexStoreDB
  )
